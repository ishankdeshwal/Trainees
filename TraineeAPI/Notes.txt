// Spring Data JPA Repository method naming conventions

Entity findByPropertyName1AndPropertyName2(PropertyType1 param1, PropertyType2 param2);


e.g Employee findByNameAndSalary(String name, Double salary);
Employee findByNameAOrSalary(String name, Double salary);
List<Employee> findBySalaryGreaterThan(Double salary);
List<Employee> findBySalaryLessThan(Double salary);



// spy object or mock object----> in simple language it is a fake object which is used to test the functionality of the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling it.
// mock testing ---> it is a testing technique in which we create a mock object which is a fake object that simulates the behavior of a real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling the real object. It is used to test the functionality of the real object without actually calling it.

@InjectMocks  // it is used for injecting the mock object into the test class
@Mock  // it is used for creating mock objects.
@ExtendWith(MockitoExtension.class)  // it is for enabling the Mockito framework in the test class, it is used to create mock objects and inject them into the test class.